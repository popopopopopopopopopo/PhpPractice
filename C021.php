<?php

$iki=explode(' ',trim(fgets(STDIN)));     //暴風域
$radius_x=$iki[2];
$radius_y=$iki[3];
$n=trim(fgets(STDIN));      //人数
for ($i=0; $i < $n ; $i++) {
    $zahyo=explode(' ',trim(fgets(STDIN)));
    $zahyo_x=$zahyo[0];
    $zahyo_y=$zahyo[1];

    if($range>=pow($))
}
}
function definition($iki,$zahyo){
    $range=pow(($zahyo-$radius_x),2)+pow(($zahyo_y-$radius_y),2);
    return $range;
}
